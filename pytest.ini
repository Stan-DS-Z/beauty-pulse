[pytest]
testpaths = tests
# Streamlit logs a ScriptRunContext warning for every st.* call made outside a
# script run — expected, since conftest imports the app to read HEADLINE.
filterwarnings =
    ignore::DeprecationWarning
addopts = -q
